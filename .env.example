AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
AZURE_OPENAI_KEY=<your-key>
AZURE_OPENAI_DEPLOYMENT=gpt-5.5

# Parts 6+ only — where the package-this backend API runs and which seeded
# persona the MCP server acts as. The defaults below are already baked into
# the code; uncomment only if your setup differs.
# PACKAGE_API_URL=http://localhost:8000
# PACKAGE_API_PERSONA=jim-halpert
